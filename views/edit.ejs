<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit HTML</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/">MyHTMLApp</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/logout">Log Keluar</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <h3>Edit Fail: <%= filename %></h3>

    <% if (messages.error) { %>
      <div class="alert alert-danger"><%= messages.error %></div>
    <% } %>
    <% if (messages.success) { %>
      <div class="alert alert-success"><%= messages.success %></div>
    <% } %>

    <form method="POST" action="/edit/<%= filename %>">
      <div class="mb-3">
        <textarea name="html" class="form-control" rows="15" required><%= htmlContent %></textarea>
      </div>
      <button type="submit" class="btn btn-success">Simpan Perubahan</button>
      <a href="/dashboard" class="btn btn-secondary ms-2">Kembali</a>
    </form>
  </div>
</body>
</html>
