<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Halaman Utama</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/">MyHTMLHost</a>
      <div class="d-flex">
        <% if (user) { %>
          <a href="/dashboard" class="btn btn-light me-2">Dashboard</a>
          <a href="/logout" class="btn btn-outline-light">Log Keluar</a>
        <% } else { %>
          <a href="/login" class="btn btn-light me-2">Log Masuk</a>
        <% } %>
      </div>
    </div>
  </nav>

  <header class="py-5 bg-primary text-white text-center">
    <div class="container">
      <h1>Selamat Datang ke MyHTMLHost</h1>
      <p class="lead">Simpan dan kongsi fail HTML anda dengan mudah</p>
    </div>
  </header>

  <section class="container py-5">
    <% if (messages.success) { %>
      <div class="alert alert-success"><%= messages.success %></div>
    <% } %>
    <% if (messages.error) { %>
      <div class="alert alert-danger"><%= messages.error %></div>
    <% } %>

    <div class="text-center">
      <% if (!user) { %>
        <a href="/register" class="btn btn-primary btn-lg">Daftar Sekarang</a>
      <% } else { %>
        <a href="/dashboard" class="btn btn-success btn-lg">Pergi ke Dashboard</a>
      <% } %>
    </div>
  </section>

  <footer class="bg-light text-center py-3 border-top">
    <small>&copy; <%= new Date().getFullYear() %> MyHTMLHost. Semua Hak Dilindungi.</small>
  </footer>
</body>
</html>
